# Trilist Environment Variables Example
# Copy this file to .env.local for local overrides if needed.
# Trilist operates 100% offline using IndexedDB, so no API keys are required by default.

# VITE_APP_TITLE=Trilist
